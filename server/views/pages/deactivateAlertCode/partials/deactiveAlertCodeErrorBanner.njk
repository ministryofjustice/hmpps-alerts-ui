{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{{ govukErrorSummary({
    titleText: "There is a problem",
    errorList: [
        {
            text: alertCodeErrorMessage,
            href: "#alertCode"
        } if alertCodeErrorMessage,
        {
            text: alertTypeErrorMessage,
            href: "#alertType"
        } if alertTypeErrorMessage
    ]
}) }}